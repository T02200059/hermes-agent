# owner 改动清单 · 待补录项梳理（截至 2026-09-10）

> **执行状态：本梳理已全部落地。** 清单正文现为 §1-§16 + 附录 A/B/C/D/E；
> 元数据统计已按实测值回填；§15/§16/§3.12/§4.15/§6.4/§7.20/§7.21 为本次新增章节。
> 本文档保留为本次盘点的审计轨迹：记录每个 commit 的归属判断、口径定义与复现命令。
>
> 用途：以 `owner/docs/owner改动清单.md` 的最后更新点为界，盘点其后产生、但尚未进入清单的 owner 改动，
> 给出「新建章节 / 挂靠已有章节 / 附录更新 / 元数据更正」四类落点。
> 本文档是梳理稿，不是清单正文；清单正文仍按 `§1-§16` + 附录 A/B/C/E 的既有结构维护。

---

## 一、边界与结论

### 1.1 时间边界

| 锚点 | 值 | 说明 |
|------|-----|------|
| 清单最后提交 | `f9bcb4d5a3`（2026-09-02 11:40） | `docs(owner): 记录 2026-09-02 合入 upstream/main 的解冲与验证` |
| 元数据「最后更新」 | 2026-09-02 | 仅覆盖 upstream merge，不含功能改动 |
| 附录 E 最后条目 | 2026-08-13 | `d118a77093` 补录后正文再无新增 |
| 本次盘点上界 | 2026-09-10 17:07（`9df4372591`） | 当前 HEAD |

**关键判断**：元数据的「最后更新 = 2026-09-02」具有误导性——该次提交只记录了上游 merge 的解冲与验证，**08-14 起的 owner 功能改动全部未进入清单正文**。因此实际待补录范围应按「清单正文 + 附录 E 的最后落笔日 2026-08-13」起算，而非 09-02。

### 1.2 待补录规模

以 `f9bcb4d5a3` 为界的严格区间：

- 本机作者 commit **18 个**，改动文件 **29 个**（去重）
- **2 个全新主题簇**：API Server × LDAP 身份准入、API Server 产物媒体与流式契约
- **3 处章节增补**：飞书 merge_forward / 卡片标签、投递账本、Gateway 与 TUI 稳定性
- **4 处既有章节挂靠**：`§4.13`、`§7.3`、`§7.14`、`§2.x` 或 `§6.x`
- **2 个前置缺口**：`6177923b26`（能力起点，早于界 1 天）、08-14～09-01 约 28 个本机 commit

---

## 二、新增章节提案

### §15 API Server：LDAP 身份准入与多 profile 路由（全新章节，8 commit）

当前清单对 API Server 的记载仅有一行（附录 B.3：`_owner_import` helper + 多 profile 路由端点 + send_only config），
与现已形成的完整身份准入体系严重不匹配。建议独立成章。

| 小节 | 标题 | 相关 commit | 必须写入的信息 |
|------|------|-------------|----------------|
| §15.1 | API Server identity routing（能力根） | `6177923b26` | `resolve_api_identity_route(identity)` 读 `patch_feishu_profile.yaml` 的 `identity_routes` 段；`_make_identity_routing_middleware` 读 `X-Hermes-Identity` 头、命中反代到子容器 API Server；不命中 / 未知身份透传；配置样例 |
| §15.2 | LDAP bind 二次认证（72h 免验） | `531508e317`、`6790f1ba6c`、`db78cdd871` | 认证流程：带 `X-Hermes-Identity-Password` 时按 DN 模板直接 bind；成功缓存 72h（内存 + `~/.hermes/ldap_identity_cache.json`，重启不丢）；失败 401 `ldap_auth_failed` 并清缓存；10s 负缓存防爆破；无密码且缓存有效则零 LDAP 调用放行。决策表 6 态（`allow` / `deny_bad_credentials` / `deny_reauth_required` / `deny_empty_password` / `deny_invalid_login` + fail-open）。安全要点：login 字符白名单 + RFC4514 转义防 DN 注入；空密码先拒（LDAP 空密码 = 匿名 bind 假成功）；DN 模板锚定 `cn=people`，`cn=deleted` 离职账号天然拒；密码头转发前剥离且不落日志。配置：`patch_feishu_profile.yaml` 顶层 `ldap:` 节（host `10.10.100.150:389`、LDAPS 636 实测未开放、`enforce=seen`、`cache_ttl_hours=72`、`negative_cache_seconds=10`、`fail_open_on_error=true`）。依赖：`ldap3==2.9.1`（惰性 import，缺包 fail-open，lean 安装不受影响） |
| §15.3 | seen 语义与缓存状态机（安全修复） | `8f533aaa0c` | **权限提升漏洞**：`seen`（曾认证）标记原与 72h 有效窗口耦合在同一条目，`_cache_evict`（密码轮换驱逐）整条删除会连带销毁 `seen`；攻击者只需知道 uid、发一次错误密码触发驱逐，待 10s 负缓存过期即可在 `enforce=seen` 下无密码完整冒充。修法：`seen` 解耦为独立 `_seen_logins` 集合，state file 升级 `version 2 {version, entries, seen}` 并对 v1 做迁移（已部署缓存不降级）；`_cache_evict` 只清有效窗口。回归：`test_wrong_password_eviction_preserves_seen_marker` 覆盖完整攻击链 |
| §15.4 | 代理转发加固：头剥离 + SSE 透传 | `311f553550`、`4b6d187a2b` | ① 转发头剥离列表加入 `x-hermes-identity`（与密码头同列）——原保留该头导致子容器（同挂载同一份 profile 配置）再次按 identity 解析并代理回自身，自循环超时对外表现为 503 Sub-gateway unavailable；实测带该头打 26027 → 503 + 单请求 6 条 503 爆发，不带 → 200。② identity 代理对 SSE 请求（path 以 `/chat/stream` 结尾或 body 带 `stream:true`）改 `web.StreamResponse` 逐 chunk 转发，`total` 不设上限（`sock_read=600`、`connect=10`），非流式保留 120s 预算 |
| §15.5 | 身份准入查询端点 + identity_whitelist | `b14892be7c` | `GET /v1/ldap/identity/{identity}/access` 只读 tri-state（`routed` / `whitelisted` / `allowed`），供 xy-portal 智能巡检判定是否放行 LDAP 用户对话入口，白名单单一真源；**绝不返回** `endpoint_url` / `api_key`，仅回 profile 名。`identity_routing_middleware` 增加 `identity_whitelist` 短路：命中者不反代子容器、由 root gateway 本体处理（与飞书 `user_routing.whitelist` 同语义，优先级高于 `identity_routes`）。`profile_routing.py` 新增 `is_api_identity_whitelisted()`。owner/ 模块缺失时返回 503 而非误判无权限。capabilities 端点广告新路由 |
| §15.6 | 流式 finish chunk 有效 session_id | `8d42e4c199` | 上下文压缩可在回合中途轮换 agent 会话代号；finish chunk 的 `hermes` 段在 effective id 与请求 session_id 不一致时回传，长连接客户端据此切换跟踪 |

**建议同章挂靠的辅助说明**：`_owner_import` 惰性加载契约（owner/ 删除即 fail-open，`API_SERVER_KEY` 仍是第一信任边界）。

### §16 API Server：产物媒体与流式事件契约（全新章节，3 commit）

| 小节 | 标题 | 相关 commit | 必须写入的信息 |
|------|------|-------------|----------------|
| §16.1 | MEDIA 附件下载 + finish chunk `hermes.files` | `3d9a9ceed4` | 远程 OpenAI 兼容前端读不到网关本机路径：图片仍内联 data URL，其余 `MEDIA:<path>` 登记为不透明 id，经 `GET /v1/media/{id}` 下载（注册与下载均走 `validate_media_delivery_path`）；流式 finish chunk 附带 `hermes.files` 供前端渲染附件卡。**新增文件** `gateway/platforms/api_server_media.py`（官方树内新增，非 owner/ 目录） |
| §16.2 | 媒体存储「接管字节 + 目录即索引」 | `9df4372591` | 原 `ApiMediaStore` 为纯进程内存字典且只登记产出方路径，存在两个独立失效源：① 进程退出清空索引 → 网关重启后既有产物 id 立即 404；② 文件本体留在产出方目录（线上实测 `/tmp/hermes-inspect`），profile 容器未挂载 `/tmp`，容器重建即丢。改法：注册时把字节快照进受管目录 `<HERMES_HOME>/cache/documents/api-media`（与 state.db 同卷同寿命，故下载侧路径校验照旧通过）；条目名 `<media_id>__<原始文件名>`，id / 下载文件名 / MIME 全部从目录列表推出——无内存表、无 manifest、无 schema 迁移，重启自愈；`.part` 暂存 + `os.replace` 原子落位，总是拷贝而非硬链（硬链共享 inode，`os.utime` 会扰动产出方文件 mtime，影响 `trust_recent_files` recency 判定）；保留策略 TTL 24h → 30 天，新增总量上限 2GB / 4096 条，按 mtime 最旧优先淘汰，节流用「每 32 次注册扫一次」计数；清扫只处理本店铸造的 `med_<id>__*` 与 `*.part`。配置：`gateway.api_server.media_store.{dir,ttl_hours,max_entries,max_file_mb,max_total_mb}`，env `HERMES_API_MEDIA_STORE_DIR` / `HERMES_API_MEDIA_STORE_TTL_HOURS` 优先于 config.yaml。**破坏性变更：部署后既有 `med_` id 一律失效**（原映射为进程内随机 id，无法迁移）；此后重启不影响下载 |
| §16.3 | `tool.progress` 附带 args 与输出摘要 | `943b6bf1ac` | `chat/completions` 的 `hermes.tool.progress`：`running` 时带脱敏参数、`completed` 时带截断输出，供前端 hover 详情卡区分连续终端命令 |

---

## 三、既有章节增补

| commit | 日期 | 内容要点 | 建议落点 | 补法 |
|--------|------|----------|----------|------|
| `2079a79fab` | 09-06 | ① 接收侧：`_extract_message_content` 对 `merge_forward` 消息二次拉取 `GET /im/v1/messages/{id}`（返回父消息 + N 条子消息，子消息带 `upper_message_id`），用共享 renderer 渲染为可读聊天记录，失败静默回退占位文本。② 工具侧：`feishu_doc_read` 的 `doc_token` 支持 `om_` 消息 ID，支持 `offset` / `mf_limit` 分页。渲染约定：`_render_merge_forward_entries`（`[时间] sender短码: 正文`；`@用户名` 从 mentions 按 user_id/open_id 双索引解析，post 的 at 段按 user_id 命中）；截断上限 100 条 / 40000 字符，截断时保留 message_id + offset 续读提示（不静默丢弃）；image/file 显式占位、嵌套合并转发标 `[嵌套合并转发 om_xxx]`；dict 与 lark SDK 对象两种 children 形态均支持 | **§4.13 扩写**，或新开 §4.15 | 现有 §4.13 已覆盖 `feishu_doc_read` 图片物化 / bitable / wiki，本项是同一工具的接收侧 + 消息类型扩展，建议并入 §4.13 并更新其小节标题；新增测试 `tests/owner/test_merge_forward_expansion.py`（11 用例） |
| `31a8005791` | 09-03 | `send_model_picker_card` 与 clarify 卡片走 `send_card` → lark SDK 路径，而 `hermes_profile` 按钮标签此前只在 REST 路径（`send_card_via_rest`）打上；`send_only` 容器卡片因此未带标签到达用户，点击落在主网关（唯一 WebSocket）上，其 adapter 无对应 `_model_picker_state` 条目 → 报「会话已过期，请重新执行 /providers」。修法：打标签动作移入 `send_card` 本体（序列化之前，之后 card 已成 JSON 字符串无法遍历），门控 `_connection_mode == "send_only"`（主网关 no-op），owner/feishu/card_sender 缺失时 fail-open 照发 | **§4.1 多 profile 路由** 的后续，或 §2.2.2 飞书模型选择器卡片 | 一句话挂 hash + 说明「标签注入点从 REST 路径前移到 `send_card` 本体」 |
| `d74762a04e` | 09-02 | 出站 `message_id` 此前只存在于内存（日志不打、库里不存），撤回 / 编辑 / 加表情回应拿不到 ID。`delivery_ledger` 的 `delivery_obligations` 加 `platform_message_id` 列 + 部分索引，`mark_delivered(oid, message_id=None)` 落库（传 `None` 表示不覆盖已有值——两条 UPDATE，非 COALESCE）；**新列必须同时写进 `CREATE TABLE` 与 ALTER 对账循环**（只改 DDL 时 `CREATE TABLE IF NOT EXISTS` 在既有 state.db 上是 no-op，列永远加不上）。`base.py` 透传 `SendResult.message_id`；飞书 `_finalize_send_result` 统一打 `[Feishu] Sent chat_id=… message_id=…`，一处覆盖 text/post/edit/file；`send_cmd.py` 非 json 模式输出 `sent message_id=om_xxx` 并向后兼容。已知边界：`_prune()` 只留 7 天 / 500 行，是崩溃恢复账本不是审计日志；cron/CLI 的 `_standalone_send` 绕开 gateway，日志有 id 但库里没有 | **新增 §7.21**（运行稳定性）或并入 §11 运维 | 独立小节 + 明确「其他环境 pull 后重启 gateway 即自动完成 schema 对账，无需手工 ALTER」 |
| `211614adf2` | 09-02 | 启动死锁：`_join_background_discovery()` 超时返回后守护线程仍持有 `_discovery_lock`（RLock），`discover_plugins()` 紧接着同步调 `discover_and_load()`，在 `with self._discovery_lock` 上无限阻塞 → hermes 启动停在空白屏，只有 Ctrl+C 能退。修法：`discover_plugins()` join 超时且后台线程仍存活时直接返回、不抢锁；`discover_and_load()` 改 `acquire(timeout=15)` 限时等待；`cli.py` 在滚动 / 清屏之前先打印 `Starting Hermes…` 并完成插件发现，使冷启动（checkout 大更新后 banner 快照失效）有可见反馈 | **新增 §7.20** | 独立小节；测试 `test_plugin_discovery_join_timeout.py` 覆盖「超时跳过」与「后台已结束则正常加载」两分支 |
| `98236ad2ed` | 09-03 | 容器化部署身份判定错误：node010 子 profile 容器把用户 `~/.hermes` 挂载为 `/root/.hermes`，路径推断恒返回 `default`，而容器身份由 compose 注入的 `HERMES_PROFILE` 声明；卡片按钮 `hermes_profile` 打标（card_sender）、skill gate、MCP identity、docker 环境标签等 **110 个调用点**全部拿到假 `default`。修法：`get_active_profile_name` 改为 env 优先（经 `_PROFILE_ID_RE` 白名单校验），空值 / 非法值（如 `pytest -p no:xdist` 类污染）回落路径推断；主网关 / CLI / Mac 无 env 时行为逐字节不变 | **§7.14**（profile 标签）后续挂 hash | 一句话挂 hash + 说明该修复是 `31a8005791` 有效的前提 |
| `efb42e6e39` | 09-03 | TUI live compression sync 误清 per-model `context_length`：`_apply_live_compression_config` 只读顶层 `model.context_length`，在 config 使用新版 `providers.<provider>.models.<model>.context_length` 结构且顶层 `model` 段无该字段时，每轮 turn 的 sync 会把 compressor 的 override 清成 `None`，下次 resolve 落到 256K fallback。症状：TUI statusbar 首次 resolve 显示 1m，`/new` 后欢迎信息显示 256k。修法：顶层缺省时回退 `get_custom_provider_context_length`（按 agent 当前 model + base_url 精确匹配 `providers.*.models`），保留 per-model override 且多模型不串值 | **§6.x 终端体验**（建议新 §6.4）或 §2.11 后续 | 独立小节或挂 §2.11；测试 `test_compression_config_hot_reload.py` 新增 2 例 |
| `eb6a97beb2` | 09-02 | OpenViking 结构化同步 3 周降级根因：`2587260ca1` 引入的 display label 把中文人名当 `peer_id` 发送，viking 服务端 `identifiers.py` 强校验 `^[a-zA-Z0-9_.@-]+`（peer_id 是 peers 目录 URI 路径段）→ 每轮 batch 400 → 降级到文本通道，2026-08-14 起持续（errors.log 日 8～52 次）。修法（对齐 viking `ingest/peer.py::safe_external_peer` 惯例）：新增 `_ascii_peer_slug()`——天然合法则小写原样、已知人名走登记表（杨天宝 → yangtianbao）、未登记非 ASCII → `ext-<sha1前10>`；user 侧 fallback 链 Inbound 人名 → `OPENVIKING_USER` → `unknown-user`；assistant 侧 `profile` → `_agent` → `hermes`；删除中文 fallback「过去的用户 / 过去的助手」；中文人名唯一锚点保留在 entities 实体文件 | **§7.3** OpenViking 同步召回的后续 | 一句话挂 hash + 失败模式（服务端 peer_id 校验 → 400 → 降级） |

---

## 四、附录 A / B 更新

### 4.1 附录 A（owner/ 模块职责索引）

**新增行**

| 路径 | 职责 | 侵入官方文件 |
|------|------|--------------|
| `owner/gateway/ldap_auth.py` | LDAP bind 认证门（决策表 6 态 / 72h 正缓存 + 10s 负缓存 + `seen` 集合 / fail-open / RFC4514 转义） | `gateway/platforms/api_server.py`（`_owner_import` 惰性） |
| `gateway/platforms/api_server_media.py` | 产物媒体存储（接管字节 + 目录即索引，TTL / 容量淘汰） | **官方树内 owner 新增文件**，非 owner/ 目录——建议单列或在附录 A 末加「官方树内 owner 新增文件」注记 |

**修订行**

- `owner/feishu/profile_routing.py` 职责需扩写：目前 `owner/feishu/` 行只写「飞书深度定制」，但该模块已承载 API Server 侧的 `resolve_api_identity_route()` 与 `is_api_identity_whitelisted()`，并复用 `profile_endpoints`；建议在 `owner/feishu/` 行中显式补出 API identity 的两个导出函数。
- `owner/gateway/` 行现状为「inbound_context + hygiene_compression_notice + steer_vision」，需追加 `ldap_auth`。

### 4.2 附录 B（官方文件侵入点速查）

**升级条目**

- `gateway/platforms/api_server.py` 当前挂在 **B.3 轻度侵入**（`_owner_import` helper + 多 profile 路由端点 + send_only config）。现状已远超轻度：identity routing middleware、LDAP gate、`GET /v1/media/{id}`、`GET /v1/ldap/identity/{identity}/access`、路由注册表与 capabilities 广告、`ApiMediaStore.from_config()` 构造、`hermes.files` finish chunk、`tool.progress` 扩展。**建议移入 B.1 重度侵入**，并同步更新其「相关 commit」列（`6177923b26`、`531508e317`、`311f553550`、`4b6d187a2b`、`b14892be7c`、`8d42e4c199`、`3d9a9ceed4`、`943b6bf1ac`、`9df4372591`）。

**新增行**（按侵入深度归位）

| 文件 | 侵入内容 | 建议归位 | 侵入类型 |
|------|----------|----------|----------|
| `gateway/delivery_ledger.py` | `delivery_obligations` 加 `platform_message_id` 列 + 部分索引；`CREATE TABLE` 与 ALTER 对账循环双写；`mark_delivered` 非覆盖语义 | B.2 中度 | inline 列扩展 + schema 对账 |
| `gateway/platforms/base.py` | `SendResult.message_id` 透传给 `mark_delivered`（既有行需追加） | B.1（已有行） | 薄胶水 |
| `plugins/platforms/feishu/adapter.py` | 既有 64+ 处标记需追加 `merge_forward` 展开（`_extract_message_content` + `_render_merge_forward_entries`）、`send_card` 内 `hermes_profile` 标签注入、`_finalize_send_result` 全路径 message_id 日志 | B.1（已有行） | inline |
| `tools/feishu_client_utils.py` / `tools/feishu_doc_tool.py` | `om_` 消息 ID 支持 + `offset` / `mf_limit` 分页读取 | B.3 轻度 | 薄胶水 |
| `hermes_cli/plugins.py` | 后台插件发现锁超时语义（`join` 超时直接返回 + `acquire(timeout=15)`） | B.2 中度 | inline |
| `cli.py` | 冷启动先打印 `Starting Hermes…` 并完成插件发现（既有行需追加） | B.3（已有行） | 薄胶水 |
| `hermes_cli/profiles.py` | `get_active_profile_name` 读 `HERMES_PROFILE` env 优先 + `_PROFILE_ID_RE` 校验 + 回落 | B.2 中度 | inline |
| `tui_gateway/server.py` | live compression sync 回退 `get_custom_provider_context_length`（既有行需追加） | B.2（已有行） | 薄胶水 |
| `plugins/memory/openviking/__init__.py` | `_ascii_peer_slug()` peer_id slug 化 + fallback 链 | B.2 中度 | inline |
| `pyproject.toml` / `uv.lock` | 新增 `ldap3==2.9.1`（exact-pin + uv lock 重生成） | B.3 轻度 | 依赖声明 |

### 4.3 附录 B.4 交叉覆盖表

需为 `gateway/platforms/api_server.py` 补一行迁移评估状态——目前 **未评估**，且它是本批新增侵入量最大的文件，建议列入附录 C 待评项（评估问题：identity middleware + LDAP gate 是否可迁入 owner-extensions plugin，或至少收敛为 `owner/gateway/` 内的单一委托入口）。

---

## 五、元数据与附录 E 更新

### 5.1 元数据表（§0.1）——已核实

**核实结论**：表中 4 个数字里，只有「Commit 数」在 2026-09-02 被改过（82 → 1573），其余三行（172 / ~75 / ~70）是 **2026-07-02 首次建表时的快照值，两个多月未更新**。溯源见下。

#### 5.1.1 数字来源溯源

`f7408a1df5`（2026-07-02，`docs(owner): add comprehensive owner branch change inventory (82 commits, 12 chapters + 4 appendices)`）首次建立元数据表，当时的口径是：

| 字段 | 2026-07-02 值 |
|------|----------------|
| 基点 | `upstream/main` @ `f53ba9bb5`（2026-06-29） |
| Commit 数 | 82（全部为 owner 个人定制，无 merge commit） |
| 改动文件总数 | 172（去重后） |
| owner/ 纯新增 | ~75 个文件 |
| 官方文件侵入 | ~70 个文件（含 ~20 个测试文件） |

`f9bcb4d5a3`（2026-09-02）把基点行改写为 `00b2e03c80`、并把 Commit 数改为 1573，**但 172 / ~75 / ~70 三行原样保留**——这是本次差异的直接原因。

补充事实：旧基点 `f53ba9bb5` 如今已是 HEAD 之前 **13816** 个提交（该分支重建后，旧基点被并入了上游历史），因此旧表的三行数字**在当前仓库图上已不可复现**——它们是历史快照，不是可重算的统计量。

「1573」同样不可复现。已穷举下列口径均不命中：`00b2e03c80..HEAD`（含/不含 merge）、本机作者、`--all --not 00b2e03c80`、`f53ba9bb5..00b2e03c80` 上游区间、全分支本机作者（1744）、非 merge（1644）。建议直接以实测值替换，不再追认原值。

#### 5.1.2 实测值（口径：`00b2e03c80..HEAD`，base 为文档声明的 `upstream/main`）

前置事实（决定了口径的干净程度）：

- `origin/main` = `upstream/main` = `main` = `00b2e03c80`，且是 HEAD 的祖先；
- `git rev-list --left-right --count origin/main...HEAD` = `0  358` —— **无 behind**，故区间内不含任何上游 commit，全部为 owner 侧改动；
- merge 提交 `3155512347` 的两个父为 `78269d5972`（owner 侧）与 `00b2e03c80`（即 base 本身），未引入额外上游提交。

| 字段 | 表中现值 | **实测（已回填口径）** | 复现命令 |
|------|----------|-------------------|----------|
| Commit 数 | 1573 | **358** | `git rev-list --count 00b2e03c80..HEAD` |
| ├ 其中非 merge | —（未拆分） | **352**（merge 6） | `git rev-list --count --no-merges 00b2e03c80..HEAD` |
| ├ 其中本机作者 | —（未拆分） | **355**（`杨天宝` 248 + `yangtb` 107） | `git rev-list --count --author=杨天宝 --author=yangtb 00b2e03c80..HEAD` |
| ├ 其中其他作者 | — | **3**（`Hermes Agent` 2 + `hermes` 1） | 248+107+3 = 358 自洽 |
| 改动文件总数 | 172 | **415** | `git diff --name-only -z 00b2e03c80..HEAD \| tr '\0' '\n' \| sort -u \| grep -c .` |
| owner/ 纯新增 | ~75 | **160**（基线时 `owner/` 目录为 **0** 文件，故即当前全部） | `git diff --name-only --diff-filter=A 00b2e03c80..HEAD -- owner/ \| wc -l` |
| 官方文件侵入 | ~70（含 ~20 测试） | **官方树 120**（不含测试）；若沿用旧口径「含测试」则为 **255**（= 120 + 135） | `git diff --name-only -z 00b2e03c80..HEAD \| tr '\0' '\n' \| sort -u \| grep -v '^owner/' \| grep -v '^tests/' \| grep -c .` |

文件口径完整拆分（三项之和 = 415，自洽）：

| 分类 | 文件数 | 备注 |
|------|--------|------|
| `owner/` | 160 | 基线时为 0，全部为新增 |
| `tests/` | 135 | 其中新增 61 |
| 官方树（其余） | 120 | 其中携带 `[owner]` / `[owner-patch]` 标记的 **54** 个 |

**测量注意事项**：`owner/docs/` 下含中文名的路径会被 git 加引号输出，用 `--name-only` 直接 `grep '^owner/'` 会漏计 2 个文件（表现为 `"owner/docs/...`）。上表统一用 `-z` + `tr '\0' '\n'` 规避。同时 `git diff --stat` 在文件数较多时会跳过详尽重命名检查（本次触发 `diff.renameLimit` 警告），统计文件数时应以 `--name-only` 为准。

#### 5.1.3 官方树 120 个文件的分布

`tools` 21、`agent` 21、`hermes_cli` 17、`ui-tui` 16、`gateway` 14、`plugins` 10、`cron` 3、`apps` 3、`tui_gateway` 2、`locales` 2，以及 `run_agent.py` / `pyproject.toml` / `uv.lock` / `model_tools.py` / `hermes_state.py` / `hermes_state_common.py` / `cli.py` / `acp_adapter` / `providers` 各 1。

其中 `ui-tui` 的 16 个文件（221 插入 / 39 删除）经核查是**真实 owner 改动**（含新增文件 `ui-tui/src/owner/statusBar.ts`），不是分支结构性差异造成的噪声，应计入。

#### 5.1.4 元数据表回填结果（已执行）

「官方文件侵入」口径已定为**不含 tests**。`owner/docs/owner改动清单.md` §0.1 已按实测值回填：

| 字段 | 原值 | 回填后 |
|------|------|--------|
| Commit 数 | 1573 | 358（`00b2e03c80..HEAD`；含 6 个 merge commit，本机作者 355 = `杨天宝` 248 + `yangtb` 107） |
| 改动文件总数 | 172（去重后） | 415（去重后） |
| owner/ 纯新增 | ~75 个文件 | 160 个文件（基点时 `owner/` 目录为空） |
| 官方文件侵入 | ~70 个文件（含 ~20 个测试文件） | 120 个文件（不含 tests；另计 `tests/` 135 个，其中新增 61） |
| 最后更新 | 2026-09-02 | 2026-09-10 |

同时在表下新增一行统计口径脚注，写明范围定义（`git rev-list --count 00b2e03c80..HEAD`）与文件数的去重方式，并注明「官方文件侵入」不含 tests，避免该行再次产生两义。

**未一并改动的字段**：「范围」行仍保持原内容——§15 / §16 的功能正文尚未落笔，提前写入 LDAP 身份准入与媒体存储会使该行失去对应章节支撑；待正文补录时同步追加。

### 5.2 章节索引（§0.2）

追加两行：

| 阅读目标 | 对应章节 |
|----------|----------|
| 看 API Server 身份路由、LDAP 认证、准入查询 | §15 |
| 看 API Server 产物媒体下载、流式事件契约 | §16 |

### 5.3 附录 E 变更日志

建议追加一条总括条目（置于「2026-09-02：合入 upstream/main」之上，保持倒序入表惯例）：

```
### 2026-09-10：09-02 之后 18 个本机 commit 补录（LDAP 身份准入 / 媒体存储 / 飞书 merge_forward）

- **类型**：文档补录（对照 owner 本机作者 09-02～09-10 与正文 diff）
- **新建正文**：§15（LDAP 身份准入与多 profile 路由，8 commit）、§16（产物媒体与流式契约，3 commit）
- **新建小节**：§7.20 插件发现超时死锁（`211614adf2`）、§7.21 出站 message_id 落库（`d74762a04e`）
- **已有章节后续**：§4.13 merge_forward（`2079a79fab`）、§4.1 卡片 hermes_profile 标签（`31a8005791`）、
  §7.14 HERMES_PROFILE env 优先（`98236ad2ed`）、§7.3 peer_id slug 化（`eb6a97beb2`）、§6.4 live compression per-model（`efb42e6e39`）
- **附录 A/B**：新增 owner/gateway/ldap_auth.py；api_server.py 从 B.3 升 B.1；
  新增 delivery_ledger / profiles / plugins / openviking / feishu tools / ldap3 依赖行
- **元数据**：最后更新 → 2026-09-10；统计口径另立项核对
- **刻意不记**：纯 i18n / 纯 docs / merge main / test-only
```

---

## 六、前置缺口（建议同期补录）

### 6.1 能力起点（早于界 1 天，必须补）

`6177923b26`（2026-09-01）`feat(owner): API Server identity routing 通过 X-Hermes-Identity 头路由到子 profile` —— §15 全部能力的技术根，清单中零记载。缺此条会使 §15.2-§15.6 看起来像无因之果。

### 6.2 08-14 ～ 09-01 未记录窗口（约 28 个本机 commit）

清单正文最后一次补录是 2026-08-13（`d118a77093`，覆盖 08-03～08-13）。08-14 起至 09-01 的 owner 改动同样未入正文，建议一并补录，高价值项：

| commit | 日期 | 内容 | 建议落点 |
|--------|------|------|----------|
| `c5e0f9beda` | 08-31 | `read embedded bitable blocks in feishu docx` | §4.13 后续 |
| `78269d5972` | 09-01 | 飞书 bot menu 三层去重——修复网络卡死恢复后连续触发 | §4.6 后续 |
| `7c759411d9` | 09-01 | bot menu 路由 `NameError`（`try_route_bot_menu_command` 未定义 `chat_type`） | §4.6 后续 |
| `a1604a1b20` | 09-01 | bot_menu `new` 键补充 ack 文案 | §4.6 后续 |
| `7ce75a8df5` | 09-01 | queue item 启动时发送 bottom-of-stream notice | §4.11 后续 |
| `b83d1da12c` | 08-27 | 飞书 bot_menu 新增 xy-damodel 系列模型切换菜单 | §4.6 后续 |
| `fde14c096d` | 08-27 | swagger-kanban scan 流水线增加 T4 自修复卡 | §11.9 后续 |
| `191e5a3287` | 08-27 | 飞书审批卡 operator 中文名缓存中途失效，不再泄漏 `ou_xxx` | §3.1 后续 |
| `d510908961` | 08-24 | 接通 `_classify_edit_failure`，修复编辑上限后 tool progress 散列 | §4.7 后续 |
| `0c48c2a8b6` / `56fa158790` | 08-19 | 代码审查 P0/P1/P2 修复 + follow-up（先 ack 再下媒体、缓存失效） | §3.7 安全加固增补 |
| `d7d4a0fd43` / `fa44610162` | 08-19 | 默认关闭飞书 skill_approval 人审门闩（含注释与默认一致） | §3.11 后续 |
| `c27120b97e` | 08-18 | diff card 增加 patch.yaml 开关（master + 平台级） | §4.4 后续 |
| `7515dd7e03` | 08-18 | skill 审批支持 `HERMES_PROFILE` env + profiles 通配 `*` | §3.11 后续 |
| `5e9d88bc79` | 08-18 | `skill_manage` 审批支持 `allow_skills` 白名单 | §3.11 后续 |
| `ac1a3b9f4a` | 08-17 | `swagger-kanban-run.sh` 增加 review 人审门闩模式 | §11.9 后续 |
| `61375e20c5` | 08-17 | 新增 `token_cost_estimate.py` 高峰 / 空闲分时计价费用估算脚本 | §11.1 或 §11.9 |

**按既有惯例不记**：纯 i18n（`92e0787ea6`、`c7d9889415`、`e372d685ec`、`6f6fabadfb`、`5d85ec89ac`、`43c3664c28`、`482b6a0bfb`）、纯 docs、merge main、test-only（`edbe86f71f`）。

---

## 七、待确认事项

1. **§15 / §16 是否合并**：LDAP 身份准入与产物媒体同属 API Server，但前者是安全边界、后者是交付契约，受众不同。倾向分章（与 §13 Desktop、§14 output_guard 的独立成章惯例一致）。
2. **`31a8005791` 落点**：`§4.1 多 profile 路由` 还是 `§2.2.2 飞书模型选择器卡片`——标签注入点属 profile transport 语义，倾向 §4.1。
3. **`efb42e6e39` 落点**：新增 §6.4（终端体验）还是挂 §2.11（provider model 缓存）——症状在 TUI、根因在 provider 配置层级，倾向 §6.4。
4. ~~**统计口径**~~ **已结项**：172 / ~75 / ~70 为 2026-07-02 旧基点快照，1573 不可复现；「官方文件侵入」口径定为**不含 tests**，元数据表已按 358 / 415 / 160 / 120 回填（详见 §5.1）。
5. **08-14～09-01 窗口是否本轮一并补录**：若本轮只补 09-02 之后，§15 会出现「能力起点缺失」的断层。
