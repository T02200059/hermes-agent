name: dashscope
version: 1.0.0
description: "DashScope image generation backend (Qwen-Image / Wan). Saves generated images to $HERMES_HOME/cache/images/."
author: owner
kind: backend
requires_env:
  - DASHSCOPE_API_KEY
